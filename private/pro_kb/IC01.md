# HTTPS enabled — how to fix

## Steps
1. Ensure you have a valid TLS certificate (Let's Encrypt).
2. Force HTTPS redirects (301) from http:// to https://.
3. Update canonical tags to https:// versions.
4. Update sitemap URLs.

## Examples
- Nginx: ...
- Cloudflare: ...
